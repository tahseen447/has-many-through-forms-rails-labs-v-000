<%= render @post %>
Comments:
  <% @post.comments.collect{|comment| comment.user.username }.unique!.each do |user| %>
    <%= link_to user.username, user_path(user) %>
  <% end %>

  <%= form_for @post %>
